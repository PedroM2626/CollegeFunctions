use std::io;
fn convert_any_binary_base(number: i64, base1: i64, base2: i64) -> i64 {
    //logic
    return;

}

fn convert_to_binary_base(number: i64, base: i64) -> i64 {
    let mut result = "";
    
    return;

}

// FUNCTION MAIN
fn main() {
    //testes
    let mut input = String::new();
    io::stdin()
        .read_line(&mut input)
        .expect("Failed to read line");
    println!("Hello, Rust! You entered: {}", input.trim());
}
